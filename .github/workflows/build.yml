name: build-test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5.0.1
    - uses: actions/setup-node@v6.0.0
      with:
         node-version: '22.21.1'
    - name: use yarn
      run: |
       yarn install
       yarn build